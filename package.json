{
  "name": "neon-city-support-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/finalize.mjs",
    "check": "astro check && tsc --noEmit -p tsconfig.worker.json",
    "types": "wrangler types",
    "test": "vitest run",
    "audit": "node scripts/audit.mjs",
    "cf:check": "wrangler deploy --env=\"\" --dry-run",
    "quality": "npm run check && npm test && npm run build && npm run audit && npm run cf:check",
    "build:production": "DEPLOY_ENV=production PUBLIC_DEPLOY_ENV=production astro build && DEPLOY_ENV=production node scripts/finalize.mjs",
    "audit:production": "DEPLOY_ENV=production node scripts/audit.mjs",
    "cf:check:production": "wrangler deploy --env production --dry-run",
    "quality:production": "npm run check && npm test && npm run build:production && npm run audit:production && npm run cf:check:production",
    "verify:production": "node scripts/verify-production.mjs"
  },
  "dependencies": {
    "astro": "7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.6",
    "typescript": "5.9.3",
    "vitest": "4.1.11",
    "wrangler": "4.132.0"
  }
}
